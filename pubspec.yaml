name: brilliant
description: Tipo y Zona del tablero, con sus pruebas.
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  equatable: ^2.0.5
  flutter:
    sdk: flutter

dev_dependencies:
  test: ^1.25.0